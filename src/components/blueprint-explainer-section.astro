<div id="blueprint-explainer" class="blueprint-explainer-section">
  <div class="blueprint-explainer-screen">
    <div class="blueprint-screen-glow" aria-hidden="true"></div>

    <div class="explainer-topbar glass-module">
      <div class="explainer-title-block">
        <span>Ozony Blueprint Screen</span>
        <strong>How the full system comes together</strong>
      </div>

      <div class="explainer-status-cluster" aria-label="Blueprint status">
        <div class="explainer-status is-active">
          <i></i>
          deployment path active
        </div>
        <div class="explainer-mini-stat">
          <span>mode</span>
          blueprint view
        </div>
        <div class="explainer-mini-stat">
          <span>nodes</span>
          09 online
        </div>
      </div>
    </div>

    <div class="explainer-grid">
      <div class="explainer-panel blueprint-story-panel glass-module">
        <div class="panel-topline">
          <span class="panel-kicker">Process</span>
          <span class="panel-id">Node 01</span>
        </div>

        <h2>
          From messy tech<br />
          to a clean business system.
        </h2>

        <p>
          Ozony Tech maps how your business actually works, then builds the
          Wi-Fi, firewall, network segmentation, website, and documentation
          around that workflow.
        </p>

        <div class="story-metrics" aria-label="Blueprint intake details">
          <div>
            <span>Audit</span>
            <strong>Space + devices</strong>
          </div>
          <div>
            <span>Plan</span>
            <strong>Rules + routes</strong>
          </div>
          <div>
            <span>Handoff</span>
            <strong>Docs included</strong>
          </div>
        </div>

        <div class="blueprint-steps">
          <div class="blueprint-step">
            <span>01</span>
            <div>
              <strong>Discover</strong>
              <p>Walkthrough, devices, pain points, coverage, and business needs.</p>
            </div>
            <em>scan</em>
          </div>

          <div class="blueprint-step">
            <span>02</span>
            <div>
              <strong>Design</strong>
              <p>Firewall rules, Wi-Fi layout, VLAN separation, website flow, and handoff docs.</p>
            </div>
            <em>map</em>
          </div>

          <div class="blueprint-step">
            <span>03</span>
            <div>
              <strong>Deploy</strong>
              <p>Install, configure, test, document, and leave the business with a cleaner setup.</p>
            </div>
            <em>live</em>
          </div>
        </div>
      </div>

      <div class="explainer-panel blueprint-map-panel glass-module">
        <div class="panel-topline">
          <span class="panel-kicker">System Map</span>
          <span class="panel-id">Node 02 / topology</span>
        </div>

        <div class="blueprint-map-large">
          <div class="map-node map-node-wide wan-node">
            <span>WAN</span>
            <strong>Internet Edge</strong>
            <small>ISP handoff / modem / gateway / public DNS</small>
            <div class="node-meta">
              <b>uplink</b>
              <b>monitored</b>
            </div>
          </div>

          <div class="map-link"><i></i></div>

          <div class="map-node firewall-node">
            <span>Security Layer</span>
            <strong>Firewall</strong>
            <small>rules / VPN / DNS filtering / threat blocking</small>
            <div class="node-meta">
              <b>deny by default</b>
              <b>policy routed</b>
            </div>
          </div>

          <div class="map-link"><i></i></div>

          <div class="map-node map-node-wide core-node">
            <span>Core</span>
            <strong>Business Network</strong>
            <small>switching / access points / segmentation / documentation</small>
            <div class="node-meta">
              <b>managed</b>
              <b>labeled</b>
              <b>scalable</b>
            </div>
          </div>

          <div class="map-branch-grid" aria-hidden="true">
            <i></i>
            <i></i>
            <i></i>
            <i></i>
          </div>

          <div class="map-segments">
            <div class="map-node segment-node">
              <span>VLAN 10</span>
              <strong>Staff</strong>
              <small>trusted devices / printers / admin tools</small>
            </div>

            <div class="map-node segment-node">
              <span>VLAN 20</span>
              <strong>Guest</strong>
              <small>internet only / isolated from business systems</small>
            </div>

            <div class="map-node segment-node">
              <span>VLAN 30</span>
              <strong>POS / IoT</strong>
              <small>payment, cameras, sensors, restricted access</small>
            </div>

            <div class="map-node segment-node">
              <span>VLAN 40</span>
              <strong>Admin</strong>
              <small>management access / monitoring / backups</small>
            </div>
          </div>
        </div>
      </div>

      <div class="explainer-panel blueprint-outcome-panel glass-module">
        <div class="panel-topline">
          <span class="panel-kicker">Outcome</span>
          <span class="panel-id">Node 03</span>
        </div>

        <div class="outcome-card">
          <div class="outcome-head">
            <span>Reliable Wi-Fi</span>
            <em>active</em>
          </div>
          <strong>Coverage that matches the space.</strong>
          <p>Access points, placement, roaming, and cleaner customer/staff experience.</p>
          <div class="outcome-meter"><i style="width: 72%"></i></div>
          <small>AP placement / roaming / guest access</small>
        </div>

        <div class="outcome-card">
          <div class="outcome-head">
            <span>Safer Network</span>
            <em>segmented</em>
          </div>
          <strong>Devices are separated by purpose.</strong>
          <p>Staff, guest, POS, cameras, and IoT do not all live in one flat network.</p>
          <div class="outcome-meter"><i style="width: 86%"></i></div>
          <small>VLANs / firewall rules / access control</small>
        </div>

        <div class="outcome-card">
          <div class="outcome-head">
            <span>Website Layer</span>
            <em>live</em>
          </div>
          <strong>Customers can find and contact you.</strong>
          <p>Service pages, intake flow, SEO structure, and a polished digital presence.</p>
          <div class="outcome-meter"><i style="width: 64%"></i></div>
          <small>intake / service pages / local SEO</small>
        </div>
      </div>
    </div>

    <div class="explainer-bottom glass-module">
      <span>Business Wi-Fi</span>
      <span>Firewalls</span>
      <span>VLAN Segmentation</span>
      <span>Access Points</span>
      <span>DNS Filtering</span>
      <span>IT Systems</span>
      <span>Websites</span>
      <span>Documentation</span>
    </div>
  </div>
</div>

<style>
  .blueprint-explainer-section {
    position: relative;
    z-index: 3;
    min-height: 100vh;
    height: 100vh;
    display: block;
    padding: 0;
    overflow: hidden;
    isolation: isolate;
  }

  .blueprint-explainer-screen {
    position: relative;
    width: 100%;
    height: 100%;
    display: grid;
    grid-template-rows: auto minmax(0, 1fr) auto;
    gap: clamp(0.75rem, 1.2vw, 1rem);
    padding: clamp(5.35rem, 6.4vw, 6.35rem) clamp(1rem, 2vw, 1.35rem) clamp(1rem, 2vw, 1.35rem);
    border-top: var(--hairline) solid color-mix(in srgb, var(--fg) 16%, transparent);
    border-bottom: var(--hairline) solid color-mix(in srgb, var(--fg) 16%, transparent);
    background:
      radial-gradient(circle at 20% 12%, color-mix(in srgb, var(--accent) 18%, transparent), transparent 32%),
      radial-gradient(circle at 82% 78%, color-mix(in srgb, var(--accent) 11%, transparent), transparent 38%),
      linear-gradient(180deg, color-mix(in srgb, var(--bg) 72%, transparent), color-mix(in srgb, var(--bg) 54%, transparent)),
      color-mix(in srgb, var(--card-bg) 82%, transparent);
    backdrop-filter: blur(20px) saturate(135%);
    -webkit-backdrop-filter: blur(20px) saturate(135%);
    overflow: hidden;
  }

  .blueprint-explainer-screen::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(color-mix(in srgb, var(--fg) 6%, transparent) 1px, transparent 1px),
      linear-gradient(90deg, color-mix(in srgb, var(--fg) 6%, transparent) 1px, transparent 1px),
      linear-gradient(color-mix(in srgb, var(--accent) 4%, transparent) 1px, transparent 1px),
      linear-gradient(90deg, color-mix(in srgb, var(--accent) 4%, transparent) 1px, transparent 1px);
    background-size: 2.35rem 2.35rem, 2.35rem 2.35rem, 9.4rem 9.4rem, 9.4rem 9.4rem;
    mask-image: radial-gradient(circle at center, black, transparent 86%);
    opacity: 0.72;
    pointer-events: none;
  }

  .blueprint-explainer-screen::after {
    content: "";
    position: absolute;
    inset: -18% 0 auto;
    height: 34%;
    background: linear-gradient(
      180deg,
      transparent,
      color-mix(in srgb, var(--accent) 5%, transparent),
      transparent
    );
    translate: 0 -100%;
    opacity: 0.55;
    pointer-events: none;
    animation: blueprint-scan 8.5s ease-in-out infinite;
  }

  .blueprint-screen-glow {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(90deg, color-mix(in srgb, var(--accent) 8%, transparent), transparent 16%, transparent 84%, color-mix(in srgb, var(--accent) 7%, transparent)),
      radial-gradient(circle at 50% 52%, color-mix(in srgb, var(--accent) 10%, transparent), transparent 42%);
    opacity: 0.58;
  }

  @keyframes blueprint-scan {
    0%, 62% {
      translate: 0 -100%;
    }

    100% {
      translate: 0 325%;
    }
  }

  .explainer-topbar,
  .explainer-grid,
  .explainer-bottom {
    position: relative;
    z-index: 2;
  }

  .glass-module {
    position: relative;
    border: var(--hairline) solid color-mix(in srgb, var(--fg) 15%, transparent);
    border-radius: var(--card-radius, 0.875rem);
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--card-bg) 86%, transparent),
        color-mix(in srgb, var(--bg) 30%, transparent)
      );
    box-shadow:
      0 0 2px 1px color-mix(in srgb, var(--fg) 12%, transparent) inset,
      0 0 14px 4px color-mix(in srgb, var(--fg) 5%, transparent) inset,
      0 1rem 3rem color-mix(in srgb, #000 26%, transparent);
    overflow: hidden;
  }

  .glass-module::before {
    content: "";
    position: absolute;
    inset: 0 auto auto 0;
    width: 100%;
    height: 1px;
    background: linear-gradient(
      90deg,
      var(--accent),
      color-mix(in srgb, var(--accent) 38%, transparent) 38%,
      transparent
    );
    opacity: 0.78;
    pointer-events: none;
  }

  .glass-module::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(var(--accent), var(--accent)) top left / 0.95rem 1px no-repeat,
      linear-gradient(var(--accent), var(--accent)) top left / 1px 0.95rem no-repeat,
      linear-gradient(var(--accent), var(--accent)) bottom right / 0.95rem 1px no-repeat,
      linear-gradient(var(--accent), var(--accent)) bottom right / 1px 0.95rem no-repeat;
    opacity: 0.34;
  }

  .explainer-topbar,
  .explainer-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.78rem 0.95rem;
  }

  .explainer-title-block span,
  .explainer-status,
  .explainer-mini-stat,
  .panel-kicker,
  .panel-id,
  .blueprint-step span,
  .blueprint-step em,
  .map-node span,
  .outcome-card span,
  .outcome-card em,
  .outcome-card small,
  .explainer-bottom span,
  .node-meta b,
  .story-metrics span,
  .story-metrics strong {
    font-family: var(--font-mono);
    text-transform: uppercase;
  }

  .explainer-title-block span,
  .panel-kicker,
  .blueprint-step span,
  .map-node span,
  .outcome-card span,
  .story-metrics span {
    color: var(--accent);
  }

  .explainer-title-block span,
  .explainer-status,
  .explainer-mini-stat,
  .panel-kicker,
  .panel-id,
  .blueprint-step span,
  .blueprint-step em,
  .map-node span,
  .outcome-card span,
  .outcome-card em,
  .outcome-card small,
  .explainer-bottom span,
  .node-meta b,
  .story-metrics span {
    font-size: 0.56rem;
    letter-spacing: 0.18em;
  }

  .explainer-title-block strong {
    display: block;
    margin-top: 0.22rem;
    color: var(--fg);
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 2.05vw, 2.2rem);
    font-weight: 400;
    line-height: 0.95;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .explainer-status-cluster {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 0.65rem;
    min-width: 0;
  }

  .explainer-status,
  .explainer-mini-stat {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    min-height: 1.8rem;
    padding: 0 0.72rem;
    border: var(--hairline) solid color-mix(in srgb, var(--fg) 12%, transparent);
    border-radius: 99rem;
    color: color-mix(in srgb, var(--fg) 68%, transparent);
    background: color-mix(in srgb, var(--bg) 26%, transparent);
    white-space: nowrap;
  }

  .explainer-status.is-active {
    color: var(--accent);
  }

  .explainer-status i {
    width: 0.42rem;
    height: 0.42rem;
    border-radius: 999rem;
    background: var(--accent);
    box-shadow: 0 0 1rem color-mix(in srgb, var(--accent) 82%, transparent);
    animation: blueprint-pulse 1.8s ease-in-out infinite;
  }

  .explainer-mini-stat span {
    color: var(--muted);
  }

  @keyframes blueprint-pulse {
    0%, 100% {
      opacity: 0.6;
      scale: 0.86;
    }

    50% {
      opacity: 1;
      scale: 1.15;
    }
  }

  .explainer-grid {
    min-height: 0;
    display: grid;
    grid-template-columns: minmax(15rem, 0.88fr) minmax(25rem, 1.42fr) minmax(15rem, 0.88fr);
    gap: clamp(0.75rem, 1.25vw, 1rem);
  }

  .explainer-panel,
  .map-node,
  .outcome-card {
    border-radius: clamp(0.7rem, 1vw, 1rem);
  }

  .explainer-panel {
    min-height: 0;
    padding: clamp(0.9rem, 1.35vw, 1.12rem);
  }

  .panel-topline,
  .outcome-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
  }

  .panel-topline {
    padding-bottom: 0.72rem;
    border-bottom: var(--hairline) solid color-mix(in srgb, var(--fg) 12%, transparent);
  }

  .panel-id,
  .blueprint-step em,
  .outcome-card em,
  .outcome-card small,
  .explainer-bottom span,
  .node-meta b,
  .story-metrics strong {
    color: color-mix(in srgb, var(--fg) 58%, transparent);
  }

  .blueprint-story-panel {
    display: flex;
    flex-direction: column;
    gap: clamp(0.78rem, 1.2vw, 1rem);
  }

  .blueprint-story-panel h2 {
    margin: 0;
    color: var(--fg);
    font-family: var(--font-display);
    font-size: clamp(2rem, 3.15vw, 4.1rem);
    font-weight: 400;
    line-height: 0.88;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    opacity: 1;
    translate: none;
  }

  .blueprint-story-panel p,
  .blueprint-step p,
  .outcome-card p {
    margin: 0;
    color: color-mix(in srgb, var(--fg) 63%, transparent);
    font-size: clamp(0.72rem, 0.84vw, 0.86rem);
    line-height: 1.62;
  }

  .story-metrics {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.45rem;
  }

  .story-metrics div {
    min-height: 3.05rem;
    padding: 0.62rem;
    border: var(--hairline) solid color-mix(in srgb, var(--fg) 12%, transparent);
    border-radius: 0.75rem;
    background: color-mix(in srgb, var(--bg) 28%, transparent);
  }

  .story-metrics strong {
    display: block;
    margin-top: 0.28rem;
    font-size: 0.58rem;
    letter-spacing: 0.08em;
    line-height: 1.25;
  }

  .blueprint-steps {
    position: relative;
    display: grid;
    gap: 0.58rem;
    margin-top: auto;
  }

  .blueprint-steps::before {
    content: "";
    position: absolute;
    top: 0.95rem;
    bottom: 0.95rem;
    left: 1.03rem;
    width: var(--hairline);
    background: linear-gradient(180deg, var(--accent), color-mix(in srgb, var(--accent) 18%, transparent));
    opacity: 0.7;
  }

  .blueprint-step {
    position: relative;
    display: grid;
    grid-template-columns: 2.1rem 1fr auto;
    gap: 0.65rem;
    align-items: start;
    padding: 0.74rem;
    border: var(--hairline) solid color-mix(in srgb, var(--fg) 13%, transparent);
    border-radius: 0.8rem;
    background: color-mix(in srgb, var(--card-bg) 52%, transparent);
    box-shadow: 0 0 0 1px color-mix(in srgb, var(--accent) 4%, transparent) inset;
  }

  .blueprint-step span {
    position: relative;
    z-index: 1;
    width: 1.5rem;
    height: 1.5rem;
    display: grid;
    place-items: center;
    border: var(--hairline) solid color-mix(in srgb, var(--accent) 50%, transparent);
    border-radius: 99rem;
    background: color-mix(in srgb, var(--bg) 72%, transparent);
  }

  .blueprint-step strong,
  .outcome-card strong {
    color: var(--fg);
    font-family: var(--font-display);
    font-size: clamp(1.05rem, 1.45vw, 1.7rem);
    font-weight: 400;
    line-height: 0.95;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .blueprint-step em,
  .outcome-card em {
    font-style: normal;
    color: var(--accent);
  }

  .blueprint-map-panel {
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
    gap: 0.75rem;
  }

  .blueprint-map-large {
    position: relative;
    display: grid;
    justify-items: center;
    align-content: center;
    height: 100%;
    min-height: 0;
    padding: clamp(0.35rem, 1.1vw, 0.9rem);
  }

  .blueprint-map-large::before {
    content: "";
    position: absolute;
    inset: 8% 7%;
    border-radius: 999rem;
    background:
      radial-gradient(circle at center, transparent 0 42%, color-mix(in srgb, var(--accent) 6%, transparent) 43%, transparent 44%),
      radial-gradient(circle at center, transparent 0 64%, color-mix(in srgb, var(--fg) 5%, transparent) 65%, transparent 66%);
    opacity: 0.7;
    pointer-events: none;
  }

  .map-node {
    position: relative;
    width: min(22rem, 100%);
    padding: 0.78rem 0.9rem;
    border: var(--hairline) solid color-mix(in srgb, var(--fg) 16%, transparent);
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--card-bg) 76%, transparent), color-mix(in srgb, var(--bg) 28%, transparent));
    box-shadow:
      0 0 0 1px color-mix(in srgb, var(--accent) 4%, transparent) inset,
      0 0.8rem 2.2rem color-mix(in srgb, #000 20%, transparent);
    overflow: hidden;
  }

  .map-node::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      linear-gradient(var(--accent), var(--accent)) top left / 0.75rem 1px no-repeat,
      linear-gradient(var(--accent), var(--accent)) top left / 1px 0.75rem no-repeat,
      linear-gradient(var(--accent), var(--accent)) bottom right / 0.75rem 1px no-repeat,
      linear-gradient(var(--accent), var(--accent)) bottom right / 1px 0.75rem no-repeat;
    opacity: 0.24;
  }

  .map-node-wide {
    width: min(28rem, 100%);
  }

  .firewall-node {
    border-color: color-mix(in srgb, var(--accent) 46%, transparent);
    background:
      radial-gradient(circle at 80% 20%, color-mix(in srgb, var(--accent) 16%, transparent), transparent 34%),
      color-mix(in srgb, var(--accent) 8%, transparent);
  }

  .core-node {
    border-color: color-mix(in srgb, var(--accent) 24%, transparent);
  }

  .map-node strong {
    display: block;
    margin-top: 0.25rem;
    color: var(--fg);
    font-family: var(--font-display);
    font-size: clamp(1.25rem, 1.78vw, 2.1rem);
    font-weight: 400;
    line-height: 0.95;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .map-node small {
    display: block;
    margin-top: 0.28rem;
    color: color-mix(in srgb, var(--fg) 56%, transparent);
    font-family: var(--font-mono);
    font-size: 0.58rem;
    letter-spacing: 0.08em;
    line-height: 1.35;
    text-transform: lowercase;
  }

  .node-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.35rem;
    margin-top: 0.55rem;
  }

  .node-meta b {
    padding: 0.22rem 0.35rem;
    border: var(--hairline) solid color-mix(in srgb, var(--fg) 12%, transparent);
    border-radius: 99rem;
    font-size: 0.5rem;
    font-weight: 400;
    letter-spacing: 0.08em;
    background: color-mix(in srgb, var(--bg) 28%, transparent);
  }

  .map-link {
    position: relative;
    width: var(--hairline);
    height: clamp(0.65rem, 1.05vw, 0.92rem);
    background: color-mix(in srgb, var(--accent) 22%, transparent);
    overflow: hidden;
  }

  .map-link i {
    position: absolute;
    inset: -100% 0 auto;
    height: 70%;
    background: linear-gradient(180deg, transparent, var(--accent), transparent);
    animation: map-flow 1.75s linear infinite;
  }

  @keyframes map-flow {
    to {
      translate: 0 290%;
    }
  }

  .map-branch-grid {
    width: min(31rem, 100%);
    height: 1.55rem;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  .map-branch-grid i {
    position: relative;
    justify-self: center;
    width: var(--hairline);
    height: 100%;
    background: linear-gradient(
      180deg,
      color-mix(in srgb, var(--accent) 78%, transparent),
      transparent
    );
  }

  .map-branch-grid i::after {
    content: "";
    position: absolute;
    left: 50%;
    top: 0;
    width: 0.32rem;
    height: 0.32rem;
    translate: -50% -50%;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0.8rem color-mix(in srgb, var(--accent) 65%, transparent);
    opacity: 0.85;
  }

  .map-segments {
    position: relative;
    width: 100%;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0.55rem;
  }

  .map-segments .map-node {
    min-height: 6.2rem;
  }

  .segment-node strong {
    font-size: clamp(1.12rem, 1.45vw, 1.55rem);
  }

  .blueprint-outcome-panel {
    display: grid;
    grid-template-rows: auto repeat(3, 1fr);
    gap: 0.62rem;
  }

  .outcome-card {
    position: relative;
    display: grid;
    align-content: start;
    gap: 0.45rem;
    padding: clamp(0.76rem, 1.1vw, 0.95rem);
    border: var(--hairline) solid color-mix(in srgb, var(--fg) 15%, transparent);
    background:
      linear-gradient(180deg, color-mix(in srgb, var(--card-bg) 64%, transparent), color-mix(in srgb, var(--bg) 26%, transparent));
    box-shadow: 0 0.8rem 2.2rem color-mix(in srgb, #000 18%, transparent);
  }

  .outcome-head em {
    padding: 0.2rem 0.42rem;
    border: var(--hairline) solid color-mix(in srgb, var(--accent) 34%, transparent);
    border-radius: 99rem;
    font-size: 0.48rem;
    letter-spacing: 0.12em;
    background: color-mix(in srgb, var(--accent) 7%, transparent);
  }

  .outcome-meter {
    position: relative;
    width: 100%;
    height: var(--hairline);
    margin-top: 0.25rem;
    background: color-mix(in srgb, var(--fg) 20%, transparent);
    overflow: hidden;
  }

  .outcome-meter i {
    display: block;
    height: 100%;
    background: linear-gradient(90deg, var(--accent), color-mix(in srgb, var(--accent) 35%, transparent));
  }

  .outcome-card small {
    line-height: 1.35;
  }

  .explainer-bottom {
    justify-content: center;
    flex-wrap: wrap;
    min-height: 2.25rem;
  }

  .explainer-bottom span {
    position: relative;
    padding-inline: 0.15rem;
  }

  .explainer-bottom span::before {
    content: "";
    display: inline-block;
    width: 0.26rem;
    height: 0.26rem;
    margin-right: 0.45rem;
    border-radius: 50%;
    background: color-mix(in srgb, var(--accent) 70%, transparent);
    vertical-align: 0.08rem;
  }

  @media (width <= 72em) {
    .blueprint-explainer-section {
      height: auto;
      min-height: auto;
      overflow: visible;
    }

    .blueprint-explainer-screen {
      height: auto;
      min-height: auto;
      padding-top: 5rem;
      overflow: visible;
    }

    .explainer-topbar {
      align-items: flex-start;
      flex-direction: column;
    }

    .explainer-status-cluster {
      justify-content: flex-start;
      flex-wrap: wrap;
    }

    .explainer-grid {
      grid-template-columns: 1fr;
    }

    .map-segments {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (width <= 37.5em) {
    .story-metrics,
    .map-segments {
      grid-template-columns: 1fr;
    }

    .blueprint-step {
      grid-template-columns: 2rem 1fr;
    }

    .blueprint-step em {
      grid-column: 2;
    }

    .explainer-bottom {
      justify-content: flex-start;
    }
  }
</style>
